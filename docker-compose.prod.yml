version: '3.8'

services:
  web:
    build: .
    command: >
      bash -c "
        # Wait for db to be ready
        while ! nc -z db 5432; do
          echo 'Waiting for database...'
          sleep 1
        done
        echo 'Database is up!'
        
        # Run migrations
        python manage.py migrate
        
        # Collect static files
        python manage.py collectstatic --noinput
        
        # Start server
        gunicorn config.wsgi:application --bind 0.0.0.0:8000
      "
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    env_file:
      - .env.prod
    depends_on:
      - db
    restart: always

  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=imagesharing
      - POSTGRES_USER=imagesharing_user
      - POSTGRES_PASSWORD=StrongDBPassword123!
    restart: always

  nginx:
    build: ./nginx
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
    restart: always
    environment:
      - SERVER_IP=${SERVER_IP:-localhost}

volumes:
  postgres_data:
  static_volume:
  media_volume: 